from django.urls import path

app_name = 'genomic_profiles'

urlpatterns = [
    # path('new-project/', views.create_new_genomic_profile, name='new_project_form'),

    # path('genomic_profiles-list/', views.ProjectList.as_view(template_name='genomic_profiles/genomic_profile_list.html'),
    #      name='list_all_projects'),

    # path('<int:pk>', views.ProjectDetails.as_view(), name='project_details'),

    # path('update/<int:pk>', views.ProjectUpdate.as_view(), name='project_update'),

    # path('delete/<int:pk>', views.ProjectDelete.as_view(), name='project_delete'),
]
